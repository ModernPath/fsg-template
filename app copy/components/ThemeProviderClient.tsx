'use client';

// This component is disabled - theme is now forced to dark mode globally
// All theme handling is done through the main providers.tsx file
export function ThemeProviderClient({ children }: { children: React.ReactNode }) {
  return children;
}
